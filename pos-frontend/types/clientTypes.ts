export interface Client {
    id: number;
    name: string;
    email: string;
    phoneNumber: string;
}

export interface ClientFormData {
    name: string;
    email: string;
    phoneNumber: string;
}