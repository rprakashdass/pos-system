apiVersion: v1
kind: Service
metadata:
  name: pos-backend-service
  namespace: pos
spec:
  selector:
    app: pos-backend
  ports:
    - protocol: TCP
      port: 8082
      targetPort: 8082
  type: ClusterIP